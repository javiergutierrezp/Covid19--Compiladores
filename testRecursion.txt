Programa Covid19;

funcion int fib (int : j);
var
int: a;
int: b;
int: c;
{
    si (j <= 1) entonces {
        regresa (j);
    } sino {
      regresa (fib(j-1) + fib(j-2));
    }
}


principal () 
{
  escribe(fib(2));
}
